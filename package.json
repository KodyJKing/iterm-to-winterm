{
  "name": "iterm-to-winterm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 1234",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm-run-all deploy:*",
    "deploy:parcel-build": "vite build",
    "deploy:push-gh-page": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "devDependencies": {
    "push-dir": "^0.4.1",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "fast-xml-parser": "^4.2.7",
    "npm-run-all": "^4.1.5"
  }
}